version: "0.1"
services:
  home-api:
    build: .
    restart: unless-stopped
    image: "home-api"
    env_file:
      - .env
    ports:
      - "${API_PORT}:${API_PORT}"
    volumes:
      - ${DATA_DIR}:/app/data/
    environment:
      - DATA_DIR=/app/data/
